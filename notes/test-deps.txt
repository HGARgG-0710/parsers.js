This file enumerates all the exports of the library in a way that also details their respective dependencies.
This will allow one to choose the order of testing. 
The things with NO dependencies will be tested first. 
When no such things remain, one shall move on to the next "dependency-minimum", until all things are tested 
[either via Unit Tests, or via Demos]. 

[-2] One shall only list the concrete classes or those abstract that need be tested via extension. 

[-1] The `global.Config` object DOES NOT get to be tested, for it affects the behaviour 
of the entirety of the library. Instead, it shall be tested TOGETHER with the pieces 
that it is intended to test. 

[0] Items NOT listed here are deemed internal details that are changeable and are NOT to be tested.

[1] the abstract classes with default methods ALSO get to be tested. 
	In particular, one creates an ABSTRACT test-suite that gets to be employed for ALL 
	the child classes. 

[2] The assertions must ALL be tested (that can be), by using the specialized try-catch tests (contract testing)

[3] Errors must ALL BE TESTED - specifically, INCORRECT input must ALSO be provided 
	(*all* kinds of incorrect input, so that ALL possible inputs for ALL the functions/classes/etc, 
	are handled!)

DEPENDENCIES:

1. global: 
	1. WrapperApp: 
		1.1. global.Pools.All
	
	2. Pools - ObjectPool: 
		1. Any class that is IInitializable (and is capable of being used with a Pool). 
			When CLEARING the Pools.All (or, whatever), verify for EVERY 'ObjectPool' in 
			the library, that its '.size' is 0!
	
2. internal: 
	1. Regex: 
		1. Errors: 
			1. Compiler: 
				1. defaultCompilerErrHandler - TreeStream, INode
				2. rangeKindsErrHandler - TreeStream, INode
				3. rangeBoundaryKindsErrHandler - TreeStream, INode
				4. negatedBuilderKindsErrHandler - TreeStream, INode
			2. NFA: 
				1. FactuallyEmptyRegexError - ISimpleErrorData
			3. Parser: 
				1. validateRangeNonEmpty - IStream, INode, IErrorData
				2. validateNonEmptyTypeMatch - IStream, INode, IErrorData
				3. validateHex - IStream, INode, IErrorData
				4. validateUnicodeCodeLength - IStream, INode, IErrorData
				5. validatePropertyName - IStream, INode, IErrorData
				6. validatePropertyValue - IStream, INode, IErrorData
				7. validateUnicodePropertyNameNonEmpty - IStream, INode, IErrorData
				8. validateUnicodePropertyNonEmpty - IStream, INode, IErrorData

	2. Stream: 
		1. RotationBuffer - Regex + TokenChooser [dependy is a DEMO]: 
			BENCHMARK [not a unit test, nor a demo]

			Specifically, write a new DumbRotationBuffer, implementing the EXACT same interface, 
				then UPDATE the code of 'PeekStream' to instead accept an interface IMPLEMENTING
				the thing that 'RotationBuffer' is. 

			THEN, benchmark the '.peek()' operation for varying cases. 

			Specifically, check: 
				1. memory (RotationBuffer *should* be better - but by HOW MUCH???)
				2. speed (RotationBuffer MIGHT be worse - but by HOW MUCH???)

			DEPENDING on it, do one of the following: 

				1. DumbRotationBuffer replaces the OLD RotationBuffer [you've overengineered]
					This is if RotationBuffer performance sucks ass. 
				2. BOTH stay, if they're BOTH good for a specific case (RUN MULTIPLE CASES!)
					The 'RotationBuffer' is optimized for large number of backtracking versions. 
				3. 'RotationBuffer' stays if SOMEHOW you were right: 
					And ONLY if the difference is REALLY big (like 2x, or )

			IN PARTICULAR, test this via: 
				1. enable injecting the used IRotationBuffer implementation via the 'global.Config'
					object INSIDE the 'PeekStream'
				2. checking, for each case, how it behaves

			USE the 'Regex' + 'TokenChooser' for this. 
			Use one of your demos. Two, even. 
			Pick a really LARGE input. 
			This will show how this affects the results of reading real-life data.

			*If* DumbRotationBuffer stays, either:
				1. make it the new 'RotationBuffer' (if the old one bites the dust)
				2. rename it to SimpleRotationBuffer (or smth similar, if the old one stays)
		
	3. Tree: 
		1. MultiIndex - NONE
	
	4. Utils: 
		1. OverflowCounter - NONE
		2. Size - NONE
	
3. objects: 
	1. ArrayBuilder - ArrayCollection: 
		You only uniquely test the '.get()' method, since everything else has already been tested. 
		Just reuse the test suite for the `ArrayCollection`. 
	
	2. ArrayCollection - NONE
	
	3. AutoCache - ISettable & IIndexable
		Test it with CONCRETE ISettable & IIndexable instances. 
		Reason: avoid mocks whenever possible. 
	
	4. AutoMap - NONE

	5. ByteSource: 
		1. WebByteSource (conditionally, either Parser-Demos, or NONE): 
			THIS IS 2 Demos: 
				1. A RAW one, with nothing but manual file access, and a barebone HTML page. 
				2. A PROPER ONE - one that uses the same barebone HTML page, but also PARSES 
					whatever the fuck is given via the current parser. 

			In particular, this is where you check that an IFileSource can be used in a 
			real-life development situation. 

		2. FileDescriptor - either Parser-Demo, or NONE: 
			SAME as WebByteSource (use +- THE SAME test suite for them): 
				1. Raw Demo (no parsing, just the reading of a large file)
				2. Full Demo (requires the parser Demo-s first, to test for each case how the thing will behave); 

	6. ReadingSource - IByteSource
		Test for BOTH OF THEM. 
		Reason: be thorough
	
	7. CollectionBuilder - IPrototypeCollection: 
		Verify it with ALL the library 'IPrototypeCollection's

	8. Counter - NONE

	9. Decoder: 
		1. Decoder8 - IByteSource
			Test for BOTH. 

		2. DecoderU8 - IByteSource
			Test for BOTH.

	10. FileDescrination - IEncoder: 
		Test for ALL of them. 
	
	11. Encoder: 	
		1. Encoder8 - NONE
		2. EncodeU8 - NONE

	12. EndingProvider: 
		1. StreamEndingProvider - IStream
		2. ByteSourceEndingProvider - IByteSource

	13. Error: 
		NOTE[1]: the Error-classes get to be tested manually via LOGGING to a file. 
			It's a sort of regression test, thus. 
		
		NOTE[2]: the expect, expectKind, etc - the Error-throwing verification functions are 
			checked thus: 
				1. run against N correct cases
				2. run against M incorrect cases, for each INSPECT that the resulting error is as desired
					Do that either automatically, OR by reading its properties...

		1. InvalidFileReadPositionError - NONE
		2. MissingImplementationError - NONE
		3. AbstractStaticMethodError - NONE
		4. MissingErrorDataContentError  - NONE
		5. findErrorDataDownstream - IOwningStream
		6. expect - IStream
		7. expectKind - ITypeCheckable & IDebugNamed
		8. allow - IStream
		9. skip - IStream
		10. unexpected - IStream
		11. allowKind - IStream
		12. tryReviveChild - IRenewerStream
		13. ensureChildUnrevivable - IRenewerStream
		14. ensureCurrDecimal - IStream
		15. validateId - IStream
		16. ParseError - IErrorData: 
			1. MessageBuilderParseError: 
				1. GenericParseError: 
					1. ExpectedMissingError: 
						1. ExpectedItemMissingError
						2. ExpectedKindMissingError
						3. ExpectedInItemListMissingError
						4. ExpectedInKindListMissingError
					2. UnexpectedItemError - IStream
					3. StreamStackError - ILinkedStream
						1. StreamRevivalError
							1. CannotReviveChildError
							2. ChildIsNotUnrevivable
					4. ExpectedDecimalError
					5. InvalidIdError

		17. NoPropertyHavingLocatableError:
			1. NoStatefulLocatableError - NONE
			2. NoIndexCarryingLocatableError - NONE
			3. NoPosedLocatableError - NONE

		18. XMLGenerationError - IDebugNamed

	14. ErrorData: 
		1. SimpleErrorData - NONE, use a Mock for IErrorType
			1. ALSO - use this in a parser demo
		2. FileErrorData - SimpleErrorData & StreamListErrorData (2 tests total, one for each)
			1. ALSO - use this in a parser demo [this way, 3 tests - 2 for SimpleErrorData; 1 for StreamListErrorData]
		3. StreamListErrorData - IPrintablePosition & ILinkedStream ...
			THIS *only* gets to be used in a DEMO.

		4. ErrorPosition: 
			1. LineIndexCarrying - LineIndex & ILinkedStream & IStreamLocator
				This gets used for TWO TESTS: 
					1. manual, with an ILinkedStream
					2. DEMO - in a parser, with an 'IErrorData' implementation
			2. PosCarrying - ILinkedStream & IStreamLocator
				This gets used for TWO TESTS: 
					1. manual, with an ILinkedStream
					2. DEMO - in a parser, with an 'IErrorData' implementation

	15. ErrorPrinter - NONE: 
		0. getNewline - NONE
		1. Plain - None
		2. WithLogger - ILogger

	16. Handler: 
		1. TableHandler - IIndexable
		2. WrapHandler - IIndexable

	17. HashMap: 
		1. BasicHash - NONE
		2. CurrentHash - IStream 
		3. LengthHash - NONE
		4. TokenHash - ITyped
		5. TypeofHash - NONE
		6. PeekHash - IStream & ALL of the other available hashes;
			Use only the concrete IHashClasses, NO MOCKS. 
			
		7. PlainMap: 
			1. PlainArray - NONE
			2. PlainMap - NONE
			3. PlainObject - NONE

		8. TerminalMap - IPlainMap
			TEST ALL THREE IPlainMap prior to starting this

	18. IndexMap: 
		NOTE: for EACH one, you test its' 'MidMap' static class. 
			Reason: be thorough
	
		1. ArrayMap - NONE
		2. BasicMap - NONE
		3. PredicateMap - NONE
		4. RegExpMap - NONE
		5. SetMap - NONE
		6. ObjectMap - NONE

		7. ModifiableMap - IIndexMap, use it for ALL OF THEM!
		8. TableMap - TableCarrier
		9. LiquidMap - TableCarrier
		10. TableCarrier - NONE

	19. LineIndex - NONE: 
		USE it with BOTH a DEMO, AND a Standalone UNIT test. 

		1. StringLineIndex - LineLengths
		2. LineLengths

	20. Loaded: 
		1. LoadedU8 - NONE
		2. LoadedU16LE - NONE
		3. Loaded8 - NONE

	21. Logger: 
		1. FileLogger - NONE
		2. LineJoiner - NONE
		3. TabbingProvider - NONE
		4. DynamicSpaceInserter - NONE

	22. Node: 
		1. SingleChildNode - INode
		2. ContentNode - NONE
		3. CachedContentNode - NONE
		4. RecursiveNode - INode
			Try it out with ALL THE OTHER types of INode-s
		5. TokenNode - NONE
		6. TreeValidationTable - INode: 
			1. Builder - INode
			2. TreePairGenerationTable - INode
				1. Builder - INode

	23. NodeSystem - INode
	24. ObjectPool - NONE
	25. Parametrized - AutoMap
	26. ParseableInput - NONE
	
	27. Regex - IStream: 
		NOTE [1]: test ALL the 'IStream's prior to even thinking of trying to test out this
			Reason: want to be sure that ALL of the 'IStream's behave (as can be determined)
				AS CLOSELY AS POSSIBLE to our spec. Since that, in particular, would enable 
				one to better isolate the behavioural issues (for which there is PLENTY of 
				opportunity inside of the Regex code...).

		NOTE [2]: tests of '.match()' look like so - one provides a MATCHING STRING, then the 
			MATCHED-INSIDE-STREAM, then checks for the correct value ['IStream's given are VEEERY
			diverse]. Also - there are simple tests for other methods 'toString()', etc...
		
		NOTE [3]: the tests described above are UNIT TESTS. In particular, one also employs the 
			'Regex + TokenChooser' combination for the parser demos. IN PARTICULAR, it means that 
			one will be testing the parsers in question, and thus - ALSO increasing the confidence 
			in the 'Regex' and 'TokenChooser' (and all the rest of the stuff that gets to be a part 
			of said parser tests).

		NOTE [4]: test ALL the respective stuff that is part of the 'Config' AS WELL - namely, the 
			'ExtensionMap', 'IRegexFactory', 'Extension's. USE the `NFARegexFinalizer` with different
			`captureResolvers', VERIFY that they all work correctly. The ONLY part of the config that 
			does NOT get to be tested is the 'IConcreteRegexFinalizer'. One even creates a custom 
			`IRegexFactory` (which will be, obviously, pretty hard, but still). 

		NOTE [5]: test for all the `Regex.Raw` would do ALL their respective methods - including the 
			'toString()' and the 'printLines()' thing. 

		1. NFAFinalizer: 
			1. ONLY as part of 'Regex' tests, use DIFFERENT 'captureResolvers'. 
				POSSIBLY: make the captureResolvers that you create (that also provide 
					practical use) into library exports. Replace the concrete values of 
					resolvers in tests with their names into library exports. Replace the concrete values of 
					resolvers in tests with their respective export names. 
				
		2. ExtensionMap: 
			1. EXTEND IT as part of the `IRegexFactory` test (specifically - creating your own custom factory with 
				specialized parsing extensions). 
		
		3. Raw: 
			NOTE [1]: only PARTIAL testing is possible, test ONLY the methods: 
				1. toString
				2. printLines
				3. set
				4. setShallow (on `Regex.Raw.Mult` children)
				5. printName (on `Regex.Raw.Mult/SinglePrinted` children)
				6. inheritExtensions: 
					1. create ExtensionMap instances just for this
				7. extensions
			
			NOTE [2]: for ALL the `Regex.Mult` children, ALSO test their respective `Builder` classes. 

			1. Mult: 
				1. Either - Regex.Raw
				2. Catenation - Regex.Raw
				3. NoneOf - Regex.Raw
				4. IgnoreCase
				5. NoCapture
				6. NonBoundary
				7. Boundary
			2. SinglePrinted: 
				1. Optional - Regex.Raw
				2. NoneOrMore - Regex.Raw
			3. Char - NONE
				1. Additionally, test the `make` method instead of the constructor (the Flyweight Pattern).
			4. TokenType - NONE
			5. CodeRange - NONE
			6. Anything - NONE
			7. UnicodeProperty - NONE: 
				1. Alias - NONE
			
			8. Factory - ExtensionMap, Regex.Raw [ALL OF THEM]

	28. ResourceCollection - IResource [ALL OF THEM]
		Employ for ALL the project's IResources. 
		Ensure it's truly implementation-agnostic 
			[i.e. capable of handling `IResource`s of multiple concrete types at the same time]. 

	29. SourceBuilder - ArrayCollection

	30. Stream: 
		DO NOT utilise EVERY kind of Stream for an interface test. 
		Only pick 2-3, one from EACH of the "general" parent classes (abstract)!
		The concrete implementations chosen should be different very frequently. 

		In particular, most IStream-s of the library are divided by type: 
			1. delegates [IdentityStream and its children]
			2. complex composites of IStream-s [ICompositeStream, ArrayStream (and its children), etc]
			3. complex traversal streams [PeekStream, LimitStream, SectionStream, etc]
			4. adaptor classes [IteratorStream, FiniteStream, TreeStream]
			5. contract streams, used in large contexts [MarkerStream, StorageStream, DepthMarkStream, RecursiveLimitStream, RecursiveProxyStream]
			6. source classes [SourceStream (and its children)]
			7. proxies [ProxyStream (and its children)]
			8. combiner/singleton streams [SingletonStream, and related]
			9. user-extendable Abstract Base Classes [NodeStream, RenewableStream]
			10. error handling and wrapper-based control flow [PanicStream (and its children)]
			[TODO: add this categorization INTO THE WIKI, add more points to it, make sure it is UTTERLY COMPLETE, 
				AND NOT A SINGLE 'IStream' IS LEFT UNCATEGORIZED!]

		The choosers are divided onto: 
			1. stateless choosers (simple functions)
			2. stateful choosers (complex objects)

		Some belong to SEVERAL categories, depending on what the user is doing (ex: HandlerStream and ValidatorStream). 

		VITAL: you *must* check for the ability of ALL 'IStream's being EMPTY ON INITIALIZATION!

		1. ActionList - IOwningStream
		2. Chooser: 
			1. IteratorStreamChooser - IOwnedStream
				Test several different ones before attempting this. 
				Have multiple combinations of varied stream size. 
		3. Locator: 
			1. WithPath: 
				1. Upwards - IOwnedStream
					Have multiple real-life predicates (BOTH specially made up AND already defined + exported). 

				2. Downwards - IOwningStream
					Have multiple real-life predicates (BOTH specially made up AND already defined + exported). 

				3. StatefulLocator: 
					Test for BOTH the regular AND the "caching" IPathFollower-s. 

					1. downwards - IOwnedStream
					2. upwards - IOwningStream

				4. PosCarryingLocator
					Test for BOTH the regular AND the "caching" IPathFollower-s. 

					1. downwards - IOwnedStream
					2. upwards - IOwningStream

				5. IndexCarryingLocator: 
					Test for BOTH the regular AND the "caching" IPathFollower-s. 
				
					1. downwards - IOwnedStream
					2. upwards - IOwningStream

				6. CachingLocator - IStreamLocator
					Test it for EVERY SINGLE ONE of 'IStreamLocator's, including a single 'CachingLocator + Smth'. 

				7. MarkerLocator:
					Test for BOTH the regular AND the "caching" IPathFollower-s. 
				
					1. downwards - IOwnedStream
					2. upwards - IOwningStream
					
		4. concrete: 
			1. AccumulatorStream - IdentityStream
			2. ByteStream - IByteSource
			3. CompositeStream - ILinkedStream
			4. ConcatStream - IOwnedStream
			5. DepthMarkStream - IdentityStream, IStream
			6. FilterStream - IOwnedStream
			7. FiniteStream - IOwnedStream
			8. FlattenerStream - IOwnedStream
			9. FreeStream - IOwnedStream
				+ ensure that this thing works (a basic Unit Test)
				+ demo (benchmark): a file-writing thingy that makes HUGE nodes 
					(i.e. large enough to leave a noticeable memory/performance footprint)

					try to run the demo: 
						1. WITHOUT `FreeStream` (+ use `ObjectPool`)
						2. WITH `FreeStream` (+ use `ObjectPool`)
						3. and WITHOUT `FreeStream` (no `ObjectPool`)
						4. and WITH `FreeStream` (no `ObjectPool`)

					BENCHMARK your findings, estimate how costly the `FreeStream` thing is...

			10. HandlerStream - IOwnedStream
			11. IdentityStream - IOwnedStream
			12. IndexStream - IOwnedStream
			13. InputStream - IParseable
			14. InterleaveStream - IOwnedStream
			15. LazyStream - ICharacterSource
			16. LimitStream - IOwnedStream
			17. LoopStream
			18. MarkerStream - IOwnedStream
			19. NodeStream - INode
				test (demo): try to make your own INode, 
					for which one shall extend the respective `NodeStream`

					Use the `renew()` thingy in the process...

			20. PanicStream - ILinkedStream

				+ demo: use this in one of the demos, to mimic the error-resistant parsing 
					(and multiple errors during parsing, in particular!)

			21. PeekStream - IOwnedStream
				benchmark: compare performance with the "complex" internal implementation, 
					and the trivial one that uses shifts. Details in the `benchmark.txt` file

			22. PosStrema - IOwnedStream
			23. RecursiveLimitStream - LimitStream.limits
			24. RecursiveProxyStream - StatefulProxyStream 
			25. SectionStream - IOwnedStream
				TODO [demo]: create specific 'ISectionGrabber's, 
					such as, for instance, the ones responsible for 
					counting non-comment, non-empty lines in JavaScript 
					files. 

				Maybe make said demo into a seprate minor little program. 

			26. SingletonStream
			27. StatefulProxyStream - ILinkedStream
			28. StorageStream - IdentityStream
			29. TreeStream - TreeWalker, INode
			30. ValidatorStream - IOwnedStream
			31. WriterStream - IdentityStream<string>

4. samples: 
	1. alphabet: 
		1. decimal - Alphabet
		2. binary - Alphabet
		3. hex - Alphabet
		4. isBinary - binary
		5. isDecimal - decimal
		6. isHex - hex
		7. isIdentifier - firstId, id
		8. Alphabet
		9. spaces - Alphabet
		10. hasSequence - Alphabet
		11. id - Alphabet
		12. english - Alphabet
		13. firstId - Alphabet
		14. isNonDigitId - firstId
		15. isSpace - spaces
	
	2. regex (namespace): 
		0. regex (function)
		1. contents
		2. and	
		3. assertions: 
			1. lookahead
			2. neg_lookahead
			3. lookbehind
			4. neg_lookbehind
			5. word_boundry
			6. non_word_boundry
			7. begin
			8. end
		4. charclass (namespace `regex.charclass`):
			1. charclass (the function `regex.charclass`)
			2. neg_charclass
			3. uniset_intersection
			4. uniset_subtraction
			5. string_literal
			6. digit
			7. non_digit
			8. word
			9. non_word
			10. space
			11. non_space
			12. wildcard
			13. htab
			14. cr
			15. lnfeed
			16. ffeed
			17. vtab
			18. nil
			19. uni_prop
			20. non_uni_prop
			21. caret
			22. uni_hex_2
			23. uni_hex_4
			24. uni_hex
			25. or
		5. flags: 	
			1. GlobalSearchFlag
			2. UnicodeFlag
			3. HasIndiciesFlag
			4. CaseInsensitiveFlag
			5. MultilineFlag
			6. UnicodeSetsFlag
			7. DotAllFlag
			8. StickyFlag
			9. with_flags
			10. g
			11. u
			12. d
			13. i
			14. m
			15. v
			16. s
			17. y
		6. groups: 	
			1. capture
			2. non_capture
			3. named_capture
			4. backref
			5. named_backref
		7. quantifiers: 
			1. repeat
			2. multiple
			3. indefinite
			4. non_greedy
			5. some 
			6. any
			7. maybe

	3. json: 
		1. JSONStream - HandlerStream
		2. JSONWrapper - DelimiterStream
		3. JSONGenerator - JSONStream, JSONWrapper, INode

	4. Node: 
		1. PlainNodes - TokenNode, RecursiveNode, ContentNode, SingleChildNode
		2. EitherType
	
	5. Pairs (namespace): 
		1. samples.Pairs (function)
		2. fromLinear
		3. to
		4. from  
		5. toArray

	6. ParseExtension - CompositeStream, IdentityStream, IInputStream

	7. platform: 
		note: for effectiveness, write 3 TESTS, each for each platform
			Let each of the 3 branches check that all the OTHER predicates 
			are `false`, meanwhile the one for the current platform is `true`. 
			Use `assert.strictEqual` for that

		1. isWindows
		2. isLinux
		3. isDarwin
	
	8. space: 
		1. LFStream - IOwnedStream<string>
		2. SpacelessStream - FilterStream
		3. getNewline - samples.platform.isWindows, samples.platform.isDarwin, samples.platform.isLinux
			Again, make this tests crossplatform
		4. NewlineStream - IndexStream
		5. toNewline
		6. splitNewlines
		7. CRLF
		8. LF

	9. Stream: 
		TODO: FINISH

	10. TerminalMap: 
		TODO: FINISH
	
	11. xml:	
		TODO: FINISH

	12. TokenChooser - Regex, PeekStream
	13. TreeEvaluator - INode, HandlerStream, TreeStream
	14. TreeMapper - TreeEvaluator

5. utils: 
	TODO: FINISH

6. constants: 
	TODO: FINISH

[TODO, distribute] NEW CLASSES/INTERFACES [to determine tests and dependencies for these accordingly!]: 
	1. NodeData: 
		1. WithPrintable
		2. WithSource
	2. LineIndexToStringConvertible
	3. IndexRange