1. List of demos: 
	1. basic arithmetic: [a-zA-Z]+, +*-/, (...)
		This is to prepare self for future work on things like JavaScript parser, 
			and to ensure that one *REALLY* does have all that is necessary in the 
			library for enabling these kinds of grammars. 
	2. large JSON parser: this is a lazy parser for huge JSON files
		NOTE: make this into a SEPARATE package! 
			Reason: could be useful for handling huge JSON parsers...
	3. simple JSON parser: this is a non-lazy parser, that does not 
		make use of various kinds of ParseLoopStream/ParseConcatStream, etc; 
		i.e. it's a pure ParseStream one, that loads the entire file into memory
	4. a "multiline" Regex-matcher demo (for testing Regex Extensions feature)
		The "multiline" matcher \m works like: 

			/#m(a|bc+)/ -> /\n*a\n*|\n*b\n*c+\n*/