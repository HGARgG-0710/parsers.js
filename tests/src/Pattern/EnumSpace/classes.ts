import {
	IncrementEnum,
	ConstEnum
} from "../../../../dist/src/Pattern/EnumSpace/classes.js"

import { EnumSpaceTest } from "./lib/classes.js"

EnumSpaceTest("IncrementEnum", IncrementEnum, {})
EnumSpaceTest("ConstEnum", ConstEnum, {})
